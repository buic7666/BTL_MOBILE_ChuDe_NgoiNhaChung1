import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors - Pastel Palette
  static const Color primaryLight = Color(0xFFFFF8F0);      // Kem nhạt
  static const Color primaryCream = Color(0xFFFFF0E6);      // Kem
  static const Color primaryPeach = Color(0xFFFFE5CC);      // Đào nhạt
  
  // Accent Colors
  static const Color accentPink = Color(0xFFFFB6C1);        // Hồng nhạt
  static const Color accentBlue = Color(0xFFADD8E6);        // Xanh dương nhạt
  static const Color accentGreen = Color(0xFFC8E6C9);       // Xanh lá nhạt
  static const Color accentYellow = Color(0xFFFFFACD);      // Vàng nhạt
  static const Color accentPurple = Color(0xFFE6D7F0);      // Tím nhạt
  
  // Text Colors
  static const Color textPrimary = Color(0xFF333333);       // Màu chữ chính
  static const Color textSecondary = Color(0xFF666666);     // Màu chữ phụ
  static const Color textLight = Color(0xFF999999);         // Màu chữ nhạt
  
  // Background Colors
  static const Color bgWhite = Color(0xFFFFFFFF);           // Trắng
  static const Color bgGrey = Color(0xFFF5F5F5);            // Xám nhạt
  static const Color bgLight = Color(0xFFFAFAFA);           // Trắng sáng
  
  // Status Colors
  static const Color success = Color(0xFF4CAF50);           // Xanh lá thành công
  static const Color error = Color(0xFFEF5350);             // Đỏ lỗi
  static const Color warning = Color(0xFFFFC107);           // Vàng cảnh báo
  static const Color info = Color(0xFF2196F3);              // Xanh dương thông tin
  
  // Border & Divider
  static const Color borderLight = Color(0xFFE0E0E0);       // Biên nhạt
  static const Color borderMedium = Color(0xFFBDBDBD);      // Biên trung bình
  
  // Shadow Color
  static const Color shadowColor = Color(0x1F000000);       // Bóng nhạt
}
