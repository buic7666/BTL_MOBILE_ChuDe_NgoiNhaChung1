{
  "name": "housepal-functions",
  "private": true,
  "engines": { "node": "^18" },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p .",
    "deploy": "firebase deploy --only functions",
    "serve": "firebase emulators:start --only functions",
    "lint": "eslint ."
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^4.6.2"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "eslint": "^9.13.0",
    "@types/node": "^20.11.30"
  }
}
